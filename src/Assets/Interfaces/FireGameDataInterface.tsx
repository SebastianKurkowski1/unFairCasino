export interface FireGameDataInterface {
    gameSettings: {
        margin: number
    },
    targetArea: {
        strokeStyle: string,
        lineWidth: number,
        length: number,
        height: number,
    }
}